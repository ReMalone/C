#include <stdio.h>
#include <stdlib.h>
#include <string.h>
char* reverse(char*, int, int);

int main()
{
charword[80];
scanf("%s",word);
printf(" Original Word: %s\n", word);
printf("Reveresed Word: %s\n", reverse(word, 0, strlen(word)-1));
}


char* reverse(char *buffer, int first, int last)
{
  if (first >= last)
   return buffer;
  else (first < last)
}                      